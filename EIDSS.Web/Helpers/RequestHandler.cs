using Microsoft.AspNetCore.Http;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace EIDSS.Web.Helpers
{
    public class RequestHandler
    {
        //public void HandleRequest(string key, string value)
        //{
        //    // do something for the request  
        //    //AppContext.Current.Session.Set<string>(key, value);
        //    ApplicationContext.SetCookie(key, value, 30);
        //}

        //public void RemoveRequest(string key)
        //{
        //    // do something for the request  
        //   // AppContext.Current.Session.Remove(key);
        //    ApplicationContext.RemoveCookie(key);
        //}

        //public string GetRequest(string key)
        //{
        //    // do something for the request  
        //    return ApplicationContext.Current.Session.Get<string>(key);
        //}
    }
}
