@using EIDSS.Web.Enumerations
@using EIDSS.Web.Components.Administration.Deduplication.Farm

@{
    ViewData["Title"] = Localizer.GetString(HeadingResourceKeyConstants.DeduplicationFarmPageHeading);
}

<div class="card">
    <div class="card-header">
        <h3>@Localizer.GetString(HeadingResourceKeyConstants.DeduplicationFarmPageHeading)</h3>
    </div>
    <div class="card-body">
        <component type="typeof(SearchFarmDeduplication)" render-mode="Server" param-Mode=@SearchModeEnum.Default param-CallbackUrl='string.Empty'  param-ShowSelectedRecordsIndicator="@Model.ShowSelectedRecordsIndicator"/>
    </div>
    <div class="card-footer">
    </div>
</div>
