@using EIDSS.Web.Enumerations
@using EIDSS.Web.Components.Administration.Deduplication.Person

@{
    ViewData["Title"] = Localizer.GetString(HeadingResourceKeyConstants.DeduplicationPersonPersonDeduplicationHeading);
}

<div class="card">
    <div class="card-header">
        <h3>@Localizer.GetString(HeadingResourceKeyConstants.DeduplicationPersonPersonDeduplicationHeading)</h3>
    </div>
    <div class="card-body">
        <component type="typeof(SearchPersonDeduplication)" render-mode="Server" param-Mode=@SearchModeEnum.Default param-CallbackUrl='string.Empty' param-ShowSelectedRecordsIndicator="@Model.ShowSelectedRecordsIndicator" />
    </div>
    <div class="card-footer">
    </div>
</div>
